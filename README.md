Clinic System — Starter scaffold (React Web, React Native Mobile, NestJS Backend)

Structure:
  - web/       -> React + TypeScript (Vite)
  - mobile/    -> Expo (React Native + TypeScript)
  - backend/   -> NestJS + TypeScript

Quick start (local dev):

1) Web
   cd web
   npm install
   npm run dev

2) Mobile (requires expo cli)
   cd mobile
   npm install
   npx expo start

3) Backend
   cd backend
   npm install
   npm run start:dev

Notes:
- This is a minimal scaffold with templates and example env files.
- Replace placeholders and follow each subfolder README for details.
