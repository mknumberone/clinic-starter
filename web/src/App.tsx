import React from 'react'

export default function App(){
  return (
    <div style={{padding:24,fontFamily:'Inter, system-ui'}}>
      <h1>Clinic Admin Dashboard (React + Vite)</h1>
      <p>Starter scaffold. Connect to API Gateway at <code>REACT_APP_API_URL</code></p>
    </div>
  )
}
