Web (React + TypeScript + Vite)

Commands:
  npm install
  npm run dev
  npm run build
  npm run preview

Notes:
  - Uses Vite + React + TS + Tailwind (optional).
  - Recommended libs: react-query, zustand, axios, shadcn or antd.
